import React from 'react';
import styles from './EditorPage.module.css';

const EditorPage = () => (
  <div className={styles.container}>
    <div className={styles.editorArea}>
      {/* 에디터 UI/컴포넌트 등은 여기에 추가 */}
    </div>
  </div>
);

export default EditorPage; 